function add(numOne, numTwo) {
  return numOne + numTwo;
}

function subtract(numOne, numTwo) {
  return numOne - numTwo;
}

function divide(numOne, numTwo) {
  return numOne / numTwo;
}

function multiply(numOne, numTwo) {
  return numOne * numTwo;
}

export const cal_obj = {
  add: add,
  subtract: subtract,
  divide: divide,
  multiply: multiply,
};

